# Used by the vitest suite only (server/test/**). Points at a separate database on the same
# docker-compose Postgres instance used for dev — never the dev "edunova" database.
DATABASE_URL_TEST="postgresql://edunova:edunova_dev_pw@localhost:5434/edunova_test?schema=public"
JWT_SECRET="edunova_test_jwt_secret"
