# EduNova Demo — Current State

> Last updated: 2026-09-08 — All of `rebuild-plan.md` (Phases 0–10) landed end to end: real backend, empty-by-default school, every domain module, security hardening, a passing 87-test backend suite plus a 6-test Playwright e2e suite, the legacy blob/columns fully deleted, and deployment tooling built and build-tested. **A follow-up deep audit** (`deep-audit-2026-09-08.md`) then live-tested every screen in the app and found 15 real bugs (6 Critical, 6 High, 3 Medium) — all fixed and re-verified against the full quality-gate suite (tsc/eslint/87 backend tests). See that doc for full detail; two low-priority items remain open (a product-decision duplication, and one unconfirmed-either-way verification-upload path). **The "complete ERP" roadmap** (`phase-11-plus-erp-roadmap.md`) is now **fully done** — all 7 phases (11: Employee Management, 12: Transport, 13: Alumni, 14: Hostel, 15: Library, 16: Inventory, 17: Accounting/GL) landed, integrated, and verified against the full quality-gate suite (tsc/eslint/87 backend tests) after every wave. **The advanced-features roadmap** (`phase-18-plus-advanced-roadmap.md`, 13 phases, 18–30) is in progress — Phase 18 (Syllabus & Teaching Progress) done alone; Phase 19 (Early Warning & Teaching Analytics) + Phase 21 (Financial Intelligence) done as the second wave; Phase 20 (AI Teaching & Communication) + Phase 22 (Campus Safety & Security) done as the third wave; Phase 23 (Parent Experience) + Phase 24 (Boarding & Hostel Extensions) done as the fourth wave; Phase 25 (Exam Operations) + Phase 27 (Culture & Engagement) done as the fifth wave; Phase 26 (Timetable Auto-Generation) done alone as the sixth wave; Phase 28 (Multi-School/Group Management) done alone as the seventh wave, the one genuine architecture change in the whole roadmap; Phase 29 (India Compliance & Offline Resilience) + Phase 30 (Canteen Prepaid Wallet) done in parallel as the eighth and final content wave — all integrated and verified against the full quality-gate suite. **All 13 phases of the advanced-features roadmap (18–30) are now complete.** A **final full-system regression audit** (`full-system-regression-2026-09-09.md`) then mirrored the original 6-agent deep-audit pattern across every phase (0–30) and found 2 Critical (money-integrity races in the canteen wallet and fee invoices), 2 High (a payroll GL double-post, an irreversible hard-delete where a soft-deactivate was called for), and 4 Medium bugs — all fixed, regression-tested, and live-reverified; a further set of Low-severity/narrow-scope findings were consciously left as documented limitations. See that doc for full detail. Sections below the banner describe the legacy demo modules and are historical context from before the rebuild; treat the rebuild status table above as authoritative.

## Rebuild status (read this first)

| Phase | Status | What landed |
|---|---|---|
| 0 · Reset & conventions | **Done** | Empty seed (one school + principal), `POST /api/admin/load-sample-data`, typed-confirm `POST /api/admin/reset`, `AuditLog` + Settings viewer, `server/src/modules/<entity>/{router,service,schema}` pattern with zod + `requireRole`, `src/lib/api.ts` + `useEntity`, hardcoded Overview tiles / Rickroll highlights / "Did you know" copy removed, sidebar reset buttons removed. |
| 1 · Academic structure | **Done** | Prisma: `AcademicYear`, `Term`, `Class`, `Subject`, `ClassSubject`, `Room`, `Enrollment`, `Guardian`. `/api/academic/*` CRUD (contract in `phase-0-1-contract.md`). Admin UI: Years & Terms, Classes & Sections (roster, class teacher), Subjects (+ per-class teacher assignment), Rooms. People rewritten on real entities (class dropdown, ward picker, one-time password modal). Server denormalises legacy `user.class/section/roll/subjects/wards/parentEmail` so old screens keep working. `db.terms`/`db.subjects` are derived from the tables. All legacy modules survive an empty school; `'Aarav Sharma'` / `'X-A'` / `'t3'` literals removed. |
| 1b · Boards & curriculum | **Done** | `Board`, `Grade`, `Stream`, `CurriculumSubject`; `Class` is board + grade (+ stream) + section, unique per board so CBSE X-A and ICSE X-A coexist; class subjects seeded from the curriculum with `POST /classes/:id/sync-curriculum`. UI: Boards & Grades, Curriculum (replaces Subjects), Classes with a board pill and a per-class Subjects & teachers modal. Spec: `phase-1b-boards-curriculum.md`. |
| 2 · Timetable | **Done** | `PeriodTemplate`, `TimetableEntry` (unique class×term×day×period), `TimetablePublish`, `Substitution`; `/api/timetable/*` with server-side teacher/room conflict detection (409 + `conflicts[]`), role-scoped `/me` and `/teacher/:id`, copy, publish. UI: Periods editor, Timetable Builder (draft grid, clash highlighting, copy-from, publish), data-driven Timetable for student/parent/teacher/admin, Overview next-class tile. Spec: `phase-2-timetable.md`. |
| 3 · Attendance & assessment | **Done** | `File` uploads (multer), `AttendanceSession/Record`, `StaffAttendance`, `GradeScale`, `Assessment/Mark`, `Homework/Submission`; `/api/attendance`, `/api/assessments` (report-card, ranks, grade-scales), `/api/homework`, `/api/files`; teacher scope enforced server-side. UI: Take Attendance (per period from the timetable), Attendance Mgmt (+staff, CSV), Gradebook, Create Assignment with attachments, data-driven Attendance/Marks/Ranks/Teachers, Homework with real uploads. Spec: `phase-3-attendance-assessment.md`. |
| 4 · Admissions & identity | **Done** | `Application`/`Certificate`/`BoardRegistration`/`ParentVerification`; admission approval creates student+parent+enrollment+guardian in one transaction; TC issues a certificate and ends enrollment; PDF certs/marksheets via pdfkit; forced password change, forgot/reset, admin set-password; parent verification is now staff-driven (self-verify rejected). UI: Applications pipeline, Board Registration, Verifications, Profile (self-edit, photo, password), Admin Management (create/role-change/revoke). Spec: `phase-4-admissions-identity.md`. |
| 5 · Finance | **Done** | `FeeHead/Structure/Invoice/Payment/Reminder`, `SalaryStructure/Payslip`; invoice generation is idempotent per class×term; sandbox payment gateway (Razorpay-ready behind an env flag); real fee reminders; payroll run is idempotent per month. UI: Fee Setup, Collections, Payroll, My Payslips, real payment flow, derived Fee Defaulters (fake AI-call simulation removed). Spec: `phase-5-finance.md`. |
| 6 · HR | **Done** | `LeaveType/LeaveRequest`, `Contract`, `Resignation`, `Duty`, `User.active`; login refused for inactive accounts; resignation approval with a past last-working-date deactivates the account immediately. UI: Leave Types, My Leave, Leave Approvals, My Contract, Contracts & Exit (admin), Duties. Spec: `phase-6-hr.md`. |
| 7 · Communication | **Done** | `Post/PostReaction/PostComment`, `Conversation/Participant/Message`, `Notification`, `Meeting`, `CalendarEvent`; SSE at `/api/events/stream` for live messages/notifications with poll fallback; conversation pairing rules enforced server-side; meeting links are real Jitsi URLs; calendar events carry a School/Class audience. UI: Feed (real authoring + reactions/comments), Messages (new-conversation picker, live thread, no more fake presence/auto-reply), notification bell, Meetings, audience-aware Calendar. Spec: `phase-7-communication.md`. |
| 8 · Welfare & compliance | **Done** | `HealthRecord`, `PermissionSlip/SlipResponse`, `Achievement`, `DisciplinaryCase/DisciplinaryNote`, `CallLog`, `Activity/ActivityRegistration`; `/api/health`, `/api/slips`, `/api/achievements`, `/api/discipline`, `/api/calls`, `/api/activities`, and an assembled `/api/reports/student/:id` dossier. Health-record visibility fixed (student/guardians/class-teacher/staff/admin only — previously a cross-student privacy leak); slips and discipline are class-scoped for teachers; activity registration enforces capacity with automatic waitlist promotion on cancellation. UI: real `HealthMod`/`SlipsMod`/`AchievementsMod`, `DisciplinaryCommitteeMod` with an evidence/notes timeline, a real call log (the old AI-call schedule/simulation concept is gone), `RegistrationsMod` rewired onto `/api/activities` (capacity + waitlist pill), a new `ActivitiesAdminMod` (staff/admin/superadmin) to create activities and view registrations, and `StudentReportMod` rebuilt on the single `/api/reports/student/:id` call. The liveness probe moved to `/healthz` (freed `/api/health` for the welfare route). Spec: `phase-8-welfare.md`. |
| 9 · Integrations & AI | **Done** | `AiConversation/AiMessage` (student-only `/api/ai/ask`, Claude via `@anthropic-ai/sdk`, scoped to the student's board/grade/subjects, 30/day rate limit, graceful `503 "AI tutor not configured"` with no key set — no fake/regex fallback); `Highlight` (`/api/highlights` CRUD, School/Class audience, staff/admin/superadmin write); `PushSubscription` (`/api/push/subscribe`, `/api/push/vapid-public-key`, `web-push` fan-out, no-ops without VAPID env vars); `server/src/lib/notify.ts` gained a pluggable email/SMS layer (`console`/`resend`/`msg91`, chosen by env) wired into fee reminders, password reset, admission credentials and leave decisions, plus push piggybacking on every existing in-app notification. UI: real `AIDoubtsMod` (threaded chat, no regex fake tutor), real `HighlightsMod` (YouTube/Drive embeds + admin publish/delete), a "Notifications" push-subscribe toggle on Profile. Env vars documented in `server/.env.example`. Spec: `phase-9-10-integrations-hardening.md` (Phase 9 section). |
| 10 · Hardening & launch | **Done** | Playwright e2e: `e2e/*.spec.ts` (login, setup checklist, timetable publish → student sees it, attendance → parent sees it, fee payment → drops off defaulters list) — 6/6 passing (`npm run e2e`); tests share the `/api/auth/login` rate limit (10/15min, Phase 10 hardening) with each other and with manual testing, so back-to-back full-suite runs within the same 15-minute window will 429 on their own login calls — this is the rate limiter working as intended, not a suite defect; space runs out or reuse the cached superadmin token in `node_modules/.cache/e2e-superadmin-auth.json` if it's still within its 15-minute lifetime. Legacy-field cleanup: every remaining frontend read of `User.class/section/roll/subjects/wards/parentEmail/board/salary` migrated to real academic-entity lookups (`classOf`, `wardsOf`, `Enrollment.rollNo`, `ClassSubject`); decorative `board`/`salary` fields removed from the People form entirely (superseded by the class picker and Payroll). Security: `Session` model + rotating refresh tokens (access token now 15 min, `POST /api/auth/refresh`/`logout` revoke/rotate), rate limiting on `/api/auth/*`, `helmet`, real file-type sniffing on uploads (magic bytes, not extension), `pino` request logging, `/healthz` does a real DB check. Pagination (`?limit&cursor`, `nextCursor`) on audit log, messages, notifications, and fee invoices, backward compatible. Automated tests: `server/test/*.test.ts` (vitest + supertest, 87 tests across auth/academic/timetable/attendance/admissions/fees/welfare) against a dedicated `edunova_test` database — `cd server && npm test` (also `npm test` from repo root); found and fixed a real bug (years/terms accepted an end date before the start date server-side). **Blob & legacy columns deleted**: the `SchoolData` model, `PUT /api/data`, and the ten legacy `User` columns are gone (migration `phase10_drop_legacy_columns`); `server/src/sampleData.ts` no longer imports from the frontend (`server/src/sampleConstants.ts` is the server-local port); `legacySync.ts` survived in trimmed form as `titleSync.ts` (it also derived `User.title` from real entities — a live dependency, not dead code) and `GET /api/data` survived as a slim endpoint computing `terms`/`subjects` straight from real tables (still read by ~9 legacy screens via `db.terms`/`db.subjects` — not itself legacy data, just a convenience shape). Deployment: `docker-compose.prod.yml` (Postgres + `api` + `nginx` + nightly-backup, 4 services, single-node), multi-stage `server/Dockerfile` (runs `prisma migrate deploy` on start) and root `Dockerfile` (Vite build → nginx, reverse-proxies `/api`), both build-tested locally; README "Deploy" section; `.env.example`/`server/.env.production.example` complete. Spec: `phase-9-10-integrations-hardening.md` (Phase 10 section). |
| 15 · Library management | **Done** | `Book`/`BookCopy`/`Loan`/`LibrarySettings`; `/api/library/{books,copies,loans,settings}` — catalog open to all roles, copy inventory + issue/return/fine staff-only, settings admin-write but staff-read (fixed a gap the frontend agent found — staff running Issue & Returns needs the real loan-period/limit/fine-rate numbers, not a hardcoded fallback). Loan limits and overdue fines enforced server-side, live-proven (limit rejection, correct per-day fine computation, Lost/Damaged return handling). UI: a catalog for every role, Issue & Returns + Library Settings for staff/admin, My Loans for everyone. Spec: `phase-15-library.md`. |
| 16 · Inventory / procurement | **Done** | `InventoryItem`/`StockMovement`/`Vendor`/`PurchaseOrder`/`PurchaseOrderLine`; `/api/inventory/{items,vendors,purchase-orders}` CRUD + `POST /items/:id/adjust`, `GET /items/:id/movements`, `GET /low-stock`, `POST /purchase-orders/:id/receive` — `currentStock` is denormalized and can ONLY change through a `StockMovement` (the item-update schema doesn't even accept the field; a PATCH carrying it is silently stripped, live-verified). PO receiving is per-line, full or partial, and recomputes overall status (all lines fully received → `Received`, some but not all → `PartiallyReceived`); over-receipt past a line's remaining quantity is rejected. `StockMovement.quantity` is positive for In/Out (direction implied by type) but signed for Adjustment (a correction can go either way — recount up, damage/loss down) — a deliberate deviation from the spec's literal "quantity always positive" line, since Adjustment has no second field to hang direction off of. Items: staff/admin/superadmin write, +teacher read; vendors/purchase-orders: staff/admin/superadmin only. UI: an Inventory catalog (+teacher read) with a low-stock section and a stock-movement timeline per item, Purchase Orders (create/Draft→Ordered/partial-or-full receive/cancel), Vendors — staff/admin/superadmin. Spec: `phase-16-inventory.md`. |
| 17 · Accounting / general ledger | **Done** | `Account`/`JournalEntry`/`JournalLine`; every entry (manual or auto-posted) funnels through one balanced-entry writer that sums debit/credit in integer cents and rejects on mismatch inside a transaction — proven live (an unbalanced 100/50 entry 400s; a 250.50/250.50 entry succeeds) and proven at scale (the whole ledger, 48 entries after a full sample load, sums to debit = credit exactly, verified by paginating and summing in Python). An 11-account starter chart is seeded (Bank/Fee Income/Salary Expense marked `isSystem`, hard-deletion-blocked). Fee payments and payroll payslip-paid events auto-post a balanced entry via best-effort, fail-open hooks (Debit Bank / Credit Fee Income; Debit Salary Expense / Credit Bank) added as single `await` lines at each module's existing success point — neither Fees' nor Payroll's core logic, return shape, or error path was touched, confirmed by the unchanged 87/87 backend test pass. `/api/accounting/{accounts,journal-entries}` CRUD (cursor-paginated) + `/reports/{trial-balance,profit-and-loss,balance-sheet}` — balance sheet folds accumulated net income into Equity as a synthetic "Retained Earnings" line so Assets = Liabilities + Equity holds without a period-close workflow (not built in this phase). UI: Chart of Accounts, Journal (System vs. manual entries visually distinguished, a live balanced/out-of-balance pill on the entry form using **integer-paise comparison, not float `===`**, so a rounding edge case can't silently pass as balanced), and the three reports — all admin/superadmin-only, confirmed absent from every other role's nav. This closes out the "complete ERP" roadmap (`phase-11-plus-erp-roadmap.md`) — Phases 11 through 17 are all done. Spec: `phase-17-accounting.md`. |
| 30 · Canteen prepaid wallet | **Done** | `StudentWallet` (unique per student, integer-paise balance) + `WalletTransaction` (signed-amount ledger, never a mutable running total — `balance` is a maintained cache updated in the same DB transaction as every insert, so it can never drift). Top-up reuses the existing Payment Gateway confirmation pattern exactly (only credits on confirmed payment success) and purchase re-checks the balance inside the DB transaction so it can never go negative — live-proven with a real ₹5,000 purchase attempt against a ₹380 balance cleanly rejected with the exact shortfall in the error message, balance unchanged afterward. Both operations post through the Phase 17 GL's one balanced-entry funnel function, never writing journal entries directly (top-up: Debit Bank / Credit Canteen Wallet Liability; purchase: Debit Canteen Wallet Liability / Credit Canteen Revenue) — a dedicated reconciliation endpoint proves, live, that the sum of every wallet's balance exactly equals the Canteen Wallet Liability account's real GL balance (both sides: ₹760), and the Phase 17 whole-ledger debit=credit check was re-run and still holds. Reused `User.id` as the scan/purchase identifier rather than inventing a new field, since no student admission-number/QR infrastructure existed to build on. UI: a parent top-up + spend-log card, a student read-only balance view, a staff point-of-sale screen (clean rejection message on insufficient balance, no transaction history exposed to counter staff — balance only), and an admin reconciliation tile. 177/177 backend tests (163 + 14 new). Spec: `phase-30-canteen-wallet.md`. |
| 29 · India compliance & offline resilience | **Done** | Two genuinely unrelated items, built by separate agents. **UDISE+ export**: a versioned, heavily-commented field-mapping file (`UDISE_MAPPING_VERSION`) built against the well-known public structure of UDISE+ forms — every "gap" (no gender/category field on any student record, no teacher qualification/training fields, no facility-checklist fields anywhere in the schema) was determined by actually checking the schema first, never guessed or invented; a real gap is reported as a gap, not silently glossed over. The disclaimer — this accelerates manual form-filling with real school data, it does not submit to any government system (no public submission API exists to integrate with even if it wanted to), and the mapping must be verified against the live portal before real use — appears in the JSON response, the CSV export, and prominently on-screen, not buried in a code comment. Live-proven: every returned count (enrollment, teachers, rooms by type) cross-checked exactly against direct database queries. **Offline-first attendance**: scoped deliberately to only the existing "Take Attendance" screen (not the whole app, per the spec's own explicit non-goal) — a hand-rolled service worker (this codebase already had one; no new PWA dependency needed) plus a real IndexedDB queue (via `idb`) for submissions that fail on a genuine network error (not just `navigator.onLine`, which is unreliable alone). Background sync replays the queue on reconnect/focus, and — since the existing attendance-submission endpoint was confirmed already idempotent (upsert-by-class/date/period, safe to retry) — the harder problem the client actually solves is conflict detection: each queued item captures a baseline snapshot and re-checks it before replay, so a session someone else already submitted while the teacher was offline surfaces a clear conflict with Overwrite/Discard actions rather than silently clobbering their change. Both the offline→online happy path and the conflict path were genuinely exercised live via Playwright + CDP network-offline simulation, not just claimed. Spec: `phase-29-india-compliance-offline.md`. |
| 28 · Multi-school / group management | **Done** | The most architecturally significant phase in this roadmap, deliberately built alone with its own design pass (mirroring the extra care Accounting/GL got earlier). `SchoolGroup` + `School.groupId` (nullable) + `GroupAdmin` — a trust/multi-campus concept layered **entirely additively** on top of the existing strict single-school architecture, where every one of the 97 Prisma models hangs off `schoolId` and `Ctx`/`requireRole()` assumed exactly one school per request. A new `assertGroupAccess` check is fully isolated from that shared scope-resolution code path — it never touches `server/src/lib/rbac.ts`/`scope.ts` — and group membership is deliberately orthogonal to a user's normal `role` (a `GroupAdmin` grant can belong to a user whose own `schoolId` isn't even a member of the group). The cross-campus overview (`GET /group/:id/overview`) is explicitly N separate calls into each member school's own real, already-existing aggregate functions (Fees, Attendance, Phase 18/19 syllabus pace and teacher workload) each with that school's own real `schoolId` — never a query spanning two schools' rows at once, preserving strict per-school data isolation while still delivering the comparison. 163/163 backend tests (142 + 21 new), explicitly re-run in full (not spot-checked) to confirm zero regression to any existing single-school behavior. Live-proven: a real two-school group with genuinely different real fee/attendance data produced a correct side-by-side comparison; a non-member got a clean 403 (no existence leak) on every `/group/*` route including a garbage groupId; a school that joined the group but whose own admin has no personal group grant still correctly 403s (school-membership ≠ user-membership); `POST /admin/reset` on one campus nulls only that school's own `groupId`, leaving the group and other member schools untouched. A real pre-existing bug was found and correctly left unfixed as out-of-scope: `employeeId` generation counts within-school but the column is globally unique, so two schools' first same-year hire collides — flagged for a future phase rather than silently patched. UI: a "Group" nav entry gated purely on membership count (not role — mirrors Phase 22's `isCounselor` splice precedent) with a group-switcher, per-school comparison cards, a tabbed drilldown, GroupAdmin-only create/join/grant-access forms (GroupViewer sees the same data read-only), and a "which group is my school part of" indicator in Settings visible even to non-members. Spec: `phase-28-multi-school-group.md`. |
| 26 · Timetable auto-generation | **Done** | A first-draft generator, deliberately not an autonomous scheduler — `POST /api/timetable/auto-generate` (admin/superadmin) greedily fills a class's (or every class's) empty periods respecting the school's real `PeriodTemplate` breaks, cross-class teacher-freeness (checked against every existing `TimetableEntry` in the school, not just the target class), Lab-room matching, double-period lab pairing, and a no-same-subject-twice-a-day rule that provably relaxes only once it's the sole way remaining demand can still fit — returns a **draft only** (`draftEntries`/`unplaced`/`conflictsAvoided`), nothing is saved. `POST /auto-generate/commit` writes the reviewed draft through the exact same `replaceGrid` path the manual "save grid" call already uses, so committed rows are indistinguishable from hand-placed ones afterward — same table, same conflict engine, same audit trail, fully editable via the ordinary click-to-edit flow. No new Prisma model or migration was needed (`ClassSubject.periodsPerWeek` already existed from earlier work). Live-proven against genuinely constrained real data: a deliberately over-loaded Chemistry requirement correctly left 7 periods in `unplaced` with a human-readable reason while placing the other 13 including 3 real double-period Lab blocks, with zero overlap against that same teacher's 21 pre-existing periods in other classes; a `fill-empty` run against a tightened PE-teacher scenario correctly deferred to that teacher's real cross-class conflicts. UI: an "Auto-Generate" entry point inside the existing Timetable Builder (no new nav item) — scope/mode picker with a required confirmation gate before the destructive "full regenerate" mode runs, a separate non-live "Draft (not saved)" review grid (dashed cells + NEW badges) with a prominent Unplaced panel and a summary strip, Commit/Discard actions, and a clear message on the rare commit-time 409 (grid changed since the draft was generated). Spec: `phase-26-timetable-autogen.md`. |
| 27 · Culture & engagement | **Done** | `HousePoints` — a real additive ledger (never a mutable running total; the leaderboard is a live SUM/groupBy query), awarded to a house resolved via its existing `Activity kind='house'` registration concept rather than adding a redundant `houseId` field anywhere; every award auto-posts a school-wide feed item through the existing Feed module, live-proven with two houses ranked correctly and matching feed posts confirmed. Student portfolio (`GET /culture/portfolio/:studentId` + a real combined PDF export, not a JSON dump) is a pure read-only aggregation of data that already existed — verified achievements, certificates, activity/house history — with zero new tables; live-proven to produce byte-identical results before and after a student is converted to an alumnus, the actual point of "connects to the Alumni module." UI: an open-to-everyone House Leaderboard (with a staff/admin-only award form folded in) and a "My Portfolio"/staff "View Portfolio" access point with a PDF download. Spec: `phase-27-culture-engagement.md`. |
| 25 · Exam operations | **Done** | `ExamSeatingPlan`/`ExamSeat` (round-robin interleave across assessments sharing a room/date, avoiding same-class+subject neighbors where the numbers allow, rejecting generation with a clear 400 if the combined roster exceeds `Room.capacity` — live-proven both ways: a deliberately too-small room correctly 400s, an adequate one produces a full non-conflicting seat chart). `InvigilationDuty` with a suggest-don't-auto-commit `/auto-assign` (excludes teachers already examining that subject, already on duty that date, or with a same-day timetable conflict; ranks remaining candidates by lowest current-term invigilation load) plus manual CRUD and self-confirm. Real hall-ticket PDFs (photo, roll number, board-registration number where applicable, assessment schedule with seat numbers pulled from any matching seating plan). Board-exam readiness dashboard aggregates Phase 18's syllabus pace with average scores and `BoardRegistration` status per student. Elective exam-clash detection reuses the timetable module's exact 409+`conflicts[]` convention rather than inventing a new error shape — live-proven by deliberately double-booking two elective subjects' exams on one date and confirming the 409, then confirming a same-class non-overlapping reschedule succeeds. One honestly-documented schema limitation carried through every check in this phase: `Assessment` has only a date, no time/period field, so every clash/free-period check operates at day granularity, noted in code comments at each call site. UI: a seating-plan generate/review/print screen, an auto-suggest + manual invigilation roster (with "My Invigilation Duties" folded into the existing Phase 6 Duties screen rather than a new nav item), a "Download Hall Ticket" button reused across the single component that already serves student/parent/staff views, and a new Readiness tab on the existing Board Registration screen. 142/142 backend tests (125 + 17 new). Spec: `phase-25-exam-operations.md`. |
| 24 · Boarding & hostel extensions | **Done** | Extends the existing Hostel module (Phase 14) rather than forking a new one. `HostelOutpass` (student/parent request → warden approve/decline, approval notifies the parent → gate-log depart/return; `Overdue` is computed at read time from `expectedReturnAt`, never a stored/scheduled status — live-proven end to end: Pending→Approved→Departed→Returned, plus a separate deliberately-overdue outpass correctly surfaced via `?status=Overdue`). `HostelRollCall`/`HostelRollCallEntry` mirror the existing student `AttendanceSession`/`AttendanceRecord` "create session, then patch records" pattern exactly — a roll-call session pre-populates every currently-active hostel allocation as present, and marking anyone absent notifies both the warden and that student's registered parent (live-proven: real `Notification` rows confirmed for both). `MessMenu`/`MealFeedback`, with a simple, honestly-scoped allergy cross-reference against the existing `HealthRecord` Allergy kind (keyword matching, not a full ingredient-tagging system) plus a "verify independently" disclaimer surfaced alongside every flag. UI: a request/approval/gate-log flow for outpasses, a nightly roll-call screen + history, a weekly mess menu editor + rating widget + warden feedback summary, and "My Outpass"/"My Mess" self-service views for students/parents. Spec: `phase-24-boarding-hostel-extensions.md`. |
| 23 · Parent experience | **Done** | `GET /api/parents/me/family-summary` — a genuine multi-ward aggregation (today's attendance, homework due this week, total fee due, next 3 calendar events, and each ward's Phase 18 syllabus-pace headline) built entirely by re-calling each domain's own existing query functions per ward rather than a parallel implementation, so all existing scoping applies unchanged; live-proven against a real 2-ward parent. Confirmed by code read that a multi-child parent previously had to pick one ward at a time everywhere (9+ places, `useWard()`/`WardPicker`) — this phase adds a genuinely new **Family Overview** landing screen without touching or replacing any of those existing per-ward screens, a deliberate scope decision. Daily digest: no scheduler infrastructure exists anywhere in this codebase (confirmed by search), so it's built as a well-tested function plus a manual-trigger endpoint (`POST /api/parents/digest/send-now`, admin/superadmin) with an explicit code-comment and doc note that real daily delivery needs an external cron — never faked as if a timer already runs it; a `ParentDigestSend` bookkeeping row prevents duplicate same-day sends. `sendWhatsApp()` added to the existing `notify.ts` provider-abstraction pattern (env-var-gated, console-log fallback, never throws) and wired into every existing email/SMS call site plus the new digest — explicitly a provider abstraction only, no real WhatsApp Business API credentials exist to test against, matching the honesty caveat the roadmap itself flagged before this phase started. Fee installment parent UI required zero backend or frontend changes beyond verification — Phase 21's installment invoices and the existing Payment Gateway screen already handle staggered per-invoice payment correctly, confirmed by a real end-to-end Playwright run showing two independently-payable "Installment · Q1/Q2" rows. 125/125 backend tests (116 + 9 new). Spec: `phase-23-parent-experience.md`. |
| 22 · Campus safety & security | **Done** | `AuthorizedPickupPerson`/`PickupEvent` (bcrypt-hashed OTP, 10-min TTL, delivered via the existing `notify.ts` email/SMS abstraction; a wrong code 400s, an unlisted/early pickup that never verifies is left explicitly `Flagged`, never silently completed), `Visitor` (check-in/checkout, "currently on campus" list), `CounselingRecord`/`CounselingSettings`/`AnonymousReport` (deliberately isolated in its own `server/src/modules/counseling/` given RBAC stricter than every existing precedent — `User.isCounselor`, counselor-only by default, optional admin oversight is an explicit off-by-default toggle and view-only even when on; live-proven that a non-counselor staff/admin account gets a clean 403/empty-list on every counseling endpoint), and `MedicationSchedule`/`MedicationLog` extending the existing Health module and reusing its RBAC scope helper verbatim. `AnonymousReport` genuinely carries no submitter identity anywhere — live-proven at the database-row level (exact column set has no id/userId field) AND at the audit-log level (its own audit trail never names who submitted it). 116/116 backend tests (87 + 29 new). UI: parent "Authorized Pickup" list, staff Pickup Desk (OTP flow) + Visitor Desk, a counselor-only Counseling Records module (gated purely on `user.isCounselor`, not role — spliced into the nav independently of the per-role registry), a "Report a Concern" anonymous form for students/parents, a Concern Review queue for admin/superadmin, and allergy alerts now surfaced prominently on the student report and a new Medication Log screen. Spec: `phase-22-campus-safety.md`. |
| 20 · AI-powered teaching & communication | **Done** | Extends the existing Claude-backed AI module (`server/src/modules/ai/`) rather than forking a parallel one. Item 1 (syllabus-scoped tutor) is a system-prompt change only — `/api/ai/ask` now injects the student's Phase 18 covered/not-yet-covered chapters with a gentle-redirect instruction, same endpoint/rate-limit/fallback. `GeneratedWorksheet` (draft via `POST /generate-worksheet`, explicitly reviewed-then-saved via a separate `POST /worksheets` — never auto-persisted). AI-drafted report-card remarks pull real report-card/attendance/achievement data and return a draft-only string; the actual save path is a new `Enrollment.remarks` JSON field (keyed by term) through its own `PUT /assessments/report-card/remark`, requiring an explicit teacher save — no AI output ever reaches a parent without that manual step, the phase's hard requirement. `POST /ai/translate` (7-language enum, rate-limited, any role) always renders alongside the original text, never replacing it. Since `ANTHROPIC_API_KEY` is unset in dev, every new endpoint's graceful 503 "not configured" behavior was what was actually live-verified (matching the existing `/ai/ask` pattern exactly, no fake/regex fallback). UI: AI Worksheets (generate → editable draft clearly labeled AI-generated → explicit Save), Report Card Remarks (Draft AI Remark → editable → explicit Save through the real publish flow), and an inline Translate control wired into the Feed composer, feed posts, and chat messages. Spec: `phase-20-ai-teaching-communication.md`. |
| 21 · Financial intelligence | **Done** | `Scholarship`/`ScholarshipAward` (Pending→Approved/Rejected, staff propose, admin/superadmin approve — approval writes real per-invoice `concession` discounts and the reduced-not-original amount is what posts to the Phase 17 GL, live-proven: a 10% scholarship dropped a ₹58,000 invoice's balance to ₹6,500→₹700, the resulting GL entry booked exactly ₹700, and the whole ledger's trial balance still summed debit=credit after). `FeeInstallmentPlan` (staggered N-invoice generation off the existing idempotent invoice-generator, reusing one `installmentLabel` per invoice so the unique constraint distinguishes them from a lump invoice; the existing Payment Gateway screen already handles paying several smaller invoices with no changes needed, confirmed by reading its logic before assuming otherwise). Three new report endpoints on `/api/accounting/reports/`: cash-flow-forecast (GL bank balance + known fee due-dates − payroll obligation, N months out), program-profitability (board+grade "programs," teaching-period-proportional expense allocation, an honest `methodology` string returned by the API and always shown verbatim in the UI, never hidden), concession-impact (scholarship vs. manual discount split). UI: three new Accounting Reports tabs, a Scholarships module (Awards/Catalog, propose vs. approve gated by role), an installment-plan section inside Fee Setup. Spec: `phase-21-financial-intelligence.md`. |
| 19 · Early warning & teaching analytics | **Done** | `StudentRiskSnapshot` (per student×term, upserted via `POST /analytics/risk-snapshots/recompute`, not live-computed on every read) with a documented, explainable weighted formula (attendance shortfall 35pts, marks trend 20pts, marks absolute level 10pts, homework-overdue 15pts capped, open discipline cases 10pts capped, fee-overdue tiered-not-linear 10pts) and a `factors` breakdown array so a class teacher can see exactly why a score is what it is — live-proven with two real seeded students at opposite ends (score 77/High vs. score 8/Low) with sensible per-factor contributions. `AnalyticsSettings` (one-row-per-school, mirrors the `LibrarySettings` pattern) holds the admin-editable "high teacher load" threshold. Lost-instructional-time reporting reuses Phase 18's pace/lost-periods calculation verbatim rather than recomputing it. Three existing screens got targeted augmentations rather than new standalone modules, per the spec's explicit intent: Timetable Builder shows a live workload warning when assigning an already-near/over-threshold teacher to a slot; Create Assignment shows how many other subjects already have homework due that day for the class; and a new substitute-assignment UI (added to the Timetable module, which had no prior substitute-picker anywhere in the frontend) surfaces ranked free-teacher suggestions above the manual picker. UI: Students at Risk (class teacher's own class; staff/admin any class) linking into the existing student report, Lost Instructional Time report (staff/admin). Spec: `phase-19-early-warning-analytics.md`. |
| 18 · Syllabus & teaching progress | **Done** | `SyllabusChapter` (ordered, lives on `CurriculumSubject` — defined once per board/grade/subject, inherited by every section), `ChapterProgress` (per `ClassSubject`, so X-A and X-B diverge independently), `TermSyllabusTarget`, `ChapterResource` (shared teaching-resource library per chapter), `Assessment.chapterIds` (coverage-vs-learning linkage). Progress writes reuse `assertWriteClassSubject` from the Phase 10 audit verbatim — re-tested live with the exact original failure mode (a teacher writing progress for a subject they don't teach in that class → 403; the class-teacher exception still legitimately succeeds). Pace calculation (scheduled periods minus holiday/staff-absence lost periods, divided against chapter `estimatedPeriods`) was hand-verified against raw seed data with an independent script and matched the API exactly. Real NCERT Class X chapter lists seeded (Math/Physics/Chemistry), not placeholders. UI: Teaching Progress (teacher, chapter status + pace headline + resource upload), Syllabus Overview (staff/admin, pace/targets/coverage tabs), chapter management folded into the existing Curriculum screen. First phase of a new advanced-features roadmap (`phase-18-plus-advanced-roadmap.md`) — 12 more phases queued. Spec: `phase-18-syllabus-tracking.md`. |
| 14 · Hostel management | **Done** | `Hostel`/`HostelRoom`/`HostelBed`/`HostelAllocation`; `/api/hostel/{hostels,rooms,beds,allocations}` with allocate/vacate/transfer — a bed holds one active occupant and a student holds one active allocation, enforced both in the service layer (clean 409s) and with DB-level partial unique indexes as a race-condition backstop. Room capacity auto-syncs its bed count on create/resize. Reuses the existing Fees module's `FeeHead` concept for hostel fees (no separate hostel-fee system). UI: a hostel→room→bed hierarchy view + allocation flow for staff/admin, a "My Hostel" view for student/parent. Spec: `phase-14-hostel.md`. |
| 13 · Alumni management | **Done** | `AlumniProfile`/`AlumniEvent`/`AlumniEventRsvp`/`AlumniDonation`; `POST /alumni/convert-student` (deliberately leaves the student's `User` role/active status untouched, mirroring how TC issuance already only ends the enrollment — uses the previously-unused `Enrollment.status: 'graduated'` value to distinguish from a TC transfer); an opt-in `convertToAlumni` flag on `POST /applications/:id/approve` lets staff convert to alumnus right at TC-issuance time. Alumni have no portal login — event RSVPs and everything else are staff-recorded, not self-service. UI: one staff/admin/superadmin module (Directory/Events/Donations) in its own "Alumni" sidebar group. Spec: `phase-13-alumni.md`. |
| 12 · Transport management | **Done** | `Route`/`Stop`/`Vehicle`/`StudentStopAssignment`/`VehicleLocation`; `/api/transport/*` CRUD plus `POST /vehicles/:id/ping` and `GET /vehicles/:id/location` — built per the standing decision that the future KMM mobile app (not this web app) will report live driver GPS, so there's no driver-facing web screen, only the API + an admin setup UI + a parent/student "My Bus" view (a clean last-seen readout with distance, not a map — sanctioned fallback). Student-stop assignments are per boarding-type (Pickup/Drop/Both) so `GET /my-stop` returns one entry per leg. Spec: `phase-12-transport.md`. |
| 11 · Employee management | **Done** | `User.employeeId` (auto-generated `EMP-<joinYear>-<seq>`, employee roles only) + real `reportsTo` relation (cycle-checked, `GET /users/:id/reports`, `GET /users/org-chart`); `PerformanceReview` (Draft→Shared→Acknowledged, employee adds comments while Shared, then locks); `EmploymentHistoryEntry` (append-only, auto-logged from role/designation/department/salary changes — closes the earlier "no raise history" gap); `StaffConductRecord` (Reported→UnderReview→Resolved, deliberately HR/admin-only, stricter than student discipline); `EmployeeDocument` (labeled uploads) + a real `GET /users/:id/id-card.pdf`. UI: My Team, My Reviews, Team Reviews (teacher/staff/admin/superadmin), Staff Conduct (admin/superadmin only), a "Reports to" picker + phone field added to the People & Roles teacher/staff/admin form, employment-history timeline + ID-card download on Profile. Found and fixed a real pre-existing bug in the same pass: `employeeId` was never serialized to the client at all. Spec: `phase-11-employee-management.md`. First phase of the "complete ERP" roadmap (`phase-11-plus-erp-roadmap.md`) — Transport/Alumni/Hostel/Library/Inventory/Accounting queued next. |

**Auth is real now**: bcrypt + JWT (`/api/auth/login`, `/api/auth/me`). Sample-school demo accounts exist only after loading sample data.

**Quality gates**: `npx tsc -b --noEmit && npx eslint .` (frontend), `cd server && npx tsc --noEmit`, and `cd server && npm test` (189/189 vitest+supertest tests across 13 files, dedicated `edunova_test` DB) — all clean/passing at this checkpoint, post-regression-audit-fixes.

**Legacy blob status**: the `SchoolData` model and its blob are deleted entirely. `GET /api/data` is now a slim endpoint that computes `terms`/`subjects` on the fly from the real `Term`/`Subject` tables (kept only because ~9 legacy screens still read `db.terms`/`db.subjects` for convenience) — every domain, including those two, is backed by real tables.

## Build & quality gates (legacy)

| Gate | Status |
|------|--------|
| `npm install` | Passes (`.npmrc` pinned to `registry.npmjs.org`) |
| `npm run build` | Passes |
| `npm run lint` | Passes (0 errors, 0 warnings) |
| `npm run dev` | Starts on `http://localhost:3000` |

## Authentication & roles

- Demo login with 6 pre-filled accounts:
  - `principal@edunova.in` / `principal123` (superadmin)
  - `admin@edunova.in` / `admin123` (admin)
  - `staff@edunova.in` / `staff123` (staff)
  - `teacher@edunova.in` / `teacher123` (teacher)
  - `parent@edunova.in` / `parent123` (parent)
  - `student@edunova.in` / `student123` (student)
- Role hierarchy: `superadmin` > `admin` > `staff` / `teacher` > `parent` / `student`.
- Session persistence across reloads via `localStorage`.
- Superadmin can manage admins (revoke access), admins can manage staff/teachers/students/parents.

## Portal modules

| Module | Status | Notes |
|--------|--------|-------|
| Overview | Functional | Role-aware greeting cards + upcoming events + stats (defaulters, resignations, disciplinary). |
| Timetable | Functional | Modern desktop time/day grid: weekdays as rows, 09:00-17:00 time slots as columns, morning/lunch/evening breaks highlighted. Polished mobile day cards. |
| Attendance | Functional | Subject-wise + daily view for all roles; staff/admin/teacher attendance management for everyone including admins. |
| Marks & Grades | Functional | Term-wise cards. |
| Rank List | Functional | Overall + subject ranks. |
| Calendar | Functional | All roles see the same calendar; admin/staff/superadmin can edit events. |
| Teachers | Functional | Directory cards. |
| School Feed | Functional | Cleaner cards, smaller media placeholder, tag overlay, and improved desktop spacing. |
| Messages | Functional | Threads and chat render; teacher-to-parent auto-replies are now context-aware and read receipts show only on the user's own messages. |
| Event Highlights | Functional | YouTube embed placeholder. |
| AI Doubt Clearing | Functional | Rule-based answers with a wider sidebar, gradient answer card, suggestion chips, and cleaner typing state. |
| Homework | Functional | Filter by subject + term. |
| Work Upload | Functional | File upload ledger with file name, size, status, notes. |
| Permission Slips | Functional | Approve/decline with verify simulation. |
| Leave Requests | Functional | Create + approve/decline. |
| Health Records | Functional | Create + e-sign simulation. |
| Achievements | Functional | Add + list. |
| Payments & Receipts | Functional | `PaymentGatewayMod` with UPI QR, card UI, net banking; intentionally disabled on mobile viewport. |
| Meetings | Functional | Request/approve video meetings with GMeet-style links; visible to requester, teacher, admin, superadmin. |
| TC & Bonafide | Functional | Apply + approve/decline. |
| Board Registration | Functional | Validate student details before sending to CBSE/Matric boards. Students see only themselves; parents see only their wards; teachers see their class; staff/admin/superadmin see all. **Name/DOB mismatch highlighting added.** |
| Take Attendance | Functional | Uses the teacher's real class roster from the DB. |
| Create Assignment | Functional | Posts to homework list. |
| Upload Grades | Functional | Publishes scores for every student in the teacher's class. |
| People | Functional | Add/edit/revoke students, teachers, staff, parents, admins; role-aware tabs. |
| Fees | Functional | Assign fee heads. |
| Fee Defaulters & AI Calls | Functional | List dues, schedule AI calls, simulate calls, review transcripts. |
| Disciplinary Committee | Functional | Report cases, track status chain, assign actions, appeals; read-only for students/parents. |
| Student Reports | Functional | Full dossier with attendance, marks, ranks, fees, meetings, calls, disciplinary, certificates, health. |
| Work Assignment | Functional | Toggle done status + generate duties, but not assigned to specific people. |
| Salary Receipts | Functional | Same as payments. |
| Contract & Resignation | Functional | Admin/superadmin manage contracts; resignation must be approved before notice starts. |
| Admin Management | Functional | Superadmin-only; revoke admin access. |

## What is simulated / not real

| Feature | Simulation |
|---------|------------|
| Authentication | ~~Plain string comparison~~ Real bcrypt + JWT since Phase 0. |
| AI doubt clearing | Hardcoded regex → answer map. |
| AI parent calls | Simulated voice-bot transcript and status. |
| Face scan / Aadhaar verify | Timed animation + any 4+ digit OTP. |
| File uploads | Visual only; no backend storage. |
| Receipts | `.txt` download, not PDF. |
| YouTube highlights | Removed; empty until the Phase 9 CMS. |
| Payments | No real money is deducted; simulated gateway. |
| Backend / API | Express + Prisma + Postgres (`server/`); legacy data still in a JSONB blob, academic entities in real tables. |

## Known bugs & rough edges

1. **Work assignments have no assignees** — duties are created but not linked to specific staff/teacher. `src/portal/modules/office.tsx:243-290`
2. **Light/dark overlap still reported** — some hardcoded light backgrounds exist without `dark:` counterparts, despite the theme fix pass.
3. **No automated test suite** — manual testing via role login only.
4. **Large bundle size** — Vite warns about ~1 MB uncompressed chunk.

**Already fixed in this cycle:** staff can manage teachers and students; teacher message auto-replies are context-aware; Take Attendance and Upload Grades use the real class roster; My Contract shows the logged-in user's contract; parent verification uses the logged-in parent's name/phone; attendance records are deterministic after reset; timetable, school feed, AI doubt clearing, and mobile login selector have been polished; admin applications pipeline has status notes and kind filter; People management links to student profile reports.

## Files and modules inventory

- `src/App.tsx` — routes + `/login` redirect when already authenticated.
- `src/pages/Landing.tsx` — marketing landing.
- `src/pages/Login.tsx` — role selector login + superadmin account.
- `src/portal/Portal.tsx` — portal shell + overview + module registry.
- `src/portal/ui.tsx` — shared primitives (Card, Pill, Modal, Field, Avatar, Progress, TermTabs, etc.).
- `src/portal/modules/academics.tsx` — Attendance, Marks, Rank, Calendar, Teachers.
- `src/portal/modules/timetable.tsx` — Timetable.
- `src/portal/modules/social.tsx` — Feed, Messages, Highlights, AI Doubts.
- `src/portal/modules/actions.tsx` — Homework, Slips, Leave, Health, Achievements, Payments, Work Upload.
- `src/portal/modules/office.tsx` — Take Attendance, Create Assignment, Upload Grades, Contract, Work Assignment, Registrations, Applications, People, Fees, Calendar Admin, Board Registration, Attendance Mgmt, Contracts & Resignations.
- `src/portal/modules/meetings.tsx` — Meeting requests + GMeet links.
- `src/portal/modules/feeDefaulters.tsx` — Fee defaulters + AI parent calls.
- `src/portal/modules/disciplinary.tsx` — Disciplinary committee.
- `src/portal/modules/paymentGateway.tsx` — Desktop payment gateway.
- `src/portal/modules/studentReport.tsx` — Full student profile report.
- `src/lib/data.ts` — types and seed DB.
- `src/lib/store.tsx` — state management + create/delete helpers.
- `src/lib/access.ts` — role/access helpers.
- `src/lib/theme.tsx` — dark/light toggle.
- `src/lib/pwa.tsx` — install prompt + QR.
- `src/index.css` — global styles + dark mode fixes.
- `public/sw.js` — service worker.
- `public/manifest.json` — PWA manifest.
- `.npmrc` — npm registry override.
- `eslint.config.js` — ignores generated UI/hook files.

## Next steps

- Phase 5 final QA, documentation sync, and GitHub push are complete.
- See `todos-and-further-plans.md` for the open backlog and future enhancements.
